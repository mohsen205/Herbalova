import { createTheme } from "@rneui/themed";

const theme = createTheme({
  lightColors: {
    primary: "#899656",
  },
  darkColors: {
    primary: "#344512",
  },
  mode: "light",
});

export default theme;
